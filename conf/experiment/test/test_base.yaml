# @package _global_

tile_threshold: ???

trainer:
  callbacks:
    - _target_: prostate_cancer.callbacks.CurvesCallback
      threshold: ${tile_threshold}

data:
  batch_size: 74 # Fits into A40 GPU
  num_workers: 8

model:
  lr: 0.0

metadata:
  experiment_name: Prostate
  run_name: ???
  description: ???
  hyperparams:
    batch_size: ${data.batch_size}
    num_workers: ${data.num_workers}
    carcinoma_t: ${carcinoma_roi_t}

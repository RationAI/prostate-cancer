defaults:
  - /data/datasets/dataset_base@thresholds

_target_: prostate_cancer.data.datasets.LabeledEmbeddingsDataset
uris:
  - ${train_224_tiles_uri}
embeddings_uri: ${train_224_features_uri}
stratified_filter: true
carcinoma_roi_t: ${carcinoma_roi_t}

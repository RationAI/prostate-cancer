defaults:
  - datasets@train: embeddings/train
  - datasets@val: embeddings/val
  - datasets@test: embeddings/test
  - datasets@predict: embeddings/predict

  - samplers@sampler: stratified_weighted_random

batch_size: ???
num_workers: ???

# @package _global_

defaults:
  - /experiment/train/images/base
  - /datamodule/transforms/normalize_w_augments@transforms
  - /model/google_vit_model@model
  - _self_

model_name: "Google ViT"

datamodule:
  train:
    uris:
      - ${train_data.tiles_filtered_uri_224}
  val:
    uris:
      - ${val_data.tiles_filtered_uri_224}

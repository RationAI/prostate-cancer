# @package _global_

defaults:
  - /experiment/train/images/train_base
  - /model/resnet50_model@model
  - _self_

metadata:
  run_name: Training ResNet50
  description: Training ResNet50 on MMCI Tile-Level Annotated Data

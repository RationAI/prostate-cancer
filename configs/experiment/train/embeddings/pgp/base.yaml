# @package _global_

defaults:
  - /experiment/train/embeddings/base
  - /model/pgp_model@model
  - _self_

datamodule:
  train:
    uris: 
      - ${train_data.tiles_filtered_w_pgp_uri_224}
  val:
    uris:
      - ${val_data.tiles_filtered_w_pgp_uri_224}

model_name: "PGP"

# @package _global_

defaults:
  - /experiment/train/bag_of_embeddings/base
  - /model/virchow2_mil_model@model
  - _self_

datamodule:
  train:
    uris:
      - ${train_data.tiles_filtered_w_virchow2_uri_224}
  val:
    uris:
      - ${val_data.tiles_filtered_w_virchow2_uri_224}

model_name: "Virchow2 (Hybrid-MIL)"

# @package _global_

defaults:
  - /experiment/train/bag_of_embeddings/base
  - /model/pgp_mil_model@model
  - _self_

datamodule:
  train:
    embeddings_uri: ${train_data.pgp_embeddings_uri}
  val:
    embeddings_uri: ${val_data.pgp_embeddings_uri}

model_name: "PGP (Hybrid-MIL)"

# @package _global_

defaults:
  - /callbacks/heatmap_and_aggregator@callbacks
  - /aggregator/max@aggregator
  - /experiment/predict/embeddings/virchow2/base
  - _self_

use_case: "Heatmaps & Max. Aggregation"

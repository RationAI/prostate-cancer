# @package _global_

defaults:
  - /experiment/predict/base
  - /datamodule/tile_embeddings_datamodule@datamodule
  - _self_

datamodule:
  batch_size: 512
  num_workers: 8

  predict:
    uris:
      - ${predict_data.tiles_uri_224}

model:
  tl_threshold: ${tile_threshold}

tile_threshold: ${model_data.tile_threshold}

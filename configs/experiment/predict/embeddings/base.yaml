# @package _global_

defaults:
  - /experiment/predict/base
  - /datamodule/embeddings_datamodule@datamodule
  - _self_

datamodule:
  batch_size: 512
  num_workers: 8

  predict:
    uris:
      - ${predict_data.tiles_uri_224}

# @package _global_

defaults:
  - /experiment/predict/embeddings/base
  - /model/pgp_model@model
  - /model/checkpoints/pgp@model_data
  - _self_

datamodule:
  predict:
    embeddings_uri: ${predict_data.pgp_embeddings_uri}

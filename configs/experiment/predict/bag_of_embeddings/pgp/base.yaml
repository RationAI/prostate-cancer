# @package _global_

defaults:
  - /experiment/predict/bag_of_embeddings/base
  - /model/pgp_hybrid_mil_model@model
  - /model/checkpoints/pgp_hybrid_mil@model_data
  - _self_

datamodule:
  predict:
    uris:
      - ${predict_data.tiles_filtered_w_pgp_uri_224}

# @package _global_

defaults:
  - /experiment/predict/base
  - /datamodule/bag_of_embeddings_datamodule@datamodule
  - _self_

slide_threshold: ${model_data.slide_threshold}
model:
  sl_threshold: ${slide_threshold}

datamodule:
  batch_size: 2
  num_workers: 4

  predict:
    uris:
      - ${predict_data.tiles_uri_224}

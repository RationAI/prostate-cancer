# @package _global_

defaults:
  - /experiment/predict/images/base
  - /model/resnet50_model@model
  - /model/checkpoints/mmci_tl/resnet50_stain@model_data
  - _self_

datamodule:
  predict:
    uris:
      - ${predict_data.tiles_filtered_uri_512}

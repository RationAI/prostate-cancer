# @package _global_

defaults:
  - /callbacks/aggregator@callbacks
  - /aggregator/mean_pool_max@aggregator
  - /experiment/predict/images/resnet50/stain_base
  - _self_

aggregator:
  kernel_size: 3
  extent_tile: 512
  stride: 256

use_case: "Mean Pool Max. Aggregation"

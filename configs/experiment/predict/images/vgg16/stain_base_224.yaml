# @package _global_

defaults:
  - /experiment/predict/images/base
  - /model/vgg16_model@model
  - /model/checkpoints/mmci_tl/vgg16_stain_224@model_data
  - _self_

datamodule:
  predict:
    uris:
      - ${predict_data.tiles_filtered_uri_224}

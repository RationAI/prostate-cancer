# @package _global_

defaults:
  - /experiment/predict/images/base
  - /model/vgg16_model@model
  - /model/checkpoints/mmci_tl/vgg16@model_data
  - _self_

datamodule:
  predict:
    uris:
      - ${predict_data.tiles_filtered_uri_512}

# @package _global_

defaults:
  - /callbacks/aggregator@callbacks
  - /aggregator/mean_pool_max@aggregator
  - /experiment/predict/images/vgg16/base
  - _self_

aggregator:
  kernel_size: 1
  extent_tile: 512
  stride: 256

use_case: "Mean Pool Max. Aggregation"

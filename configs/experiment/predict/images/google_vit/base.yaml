# @package _global_

defaults:
  - /experiment/predict/images/base
  - /model/google_vit_model@model
  - _self_


datamodule:
  batch_size: 96
  num_workers: 8

  predict:
    uris:
      - ${predict_data.tiles_filtered_uri_224}

# @package _global_

defaults:
  - /experiment/predict/images/base
  - /model/google_vit_model@model
  - /model/checkpoints/mmci_tl/google_vit@model_data
  - _self_


datamodule:
  batch_size: 96
  num_workers: 8

  predict:
    uris:
      - ${predict_data.tiles_uri_224}

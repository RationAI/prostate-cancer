# @package _global_

defaults:
  - /experiment/predict/predict_base
  - /data/images_datamodule@data
  - /model/resnet50_model@model
  - _self_

metadata:
  run_name: ResNet50 Predict
  description: ResNet50 Predict

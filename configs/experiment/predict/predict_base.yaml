# @package _global_
defaults:
  - /aggregator/max@aggregator
  - _self_

checkpoint: ???

trainer:
  callbacks:
    - _target_: prostate_cancer.callbacks.AggregatorCallback
      aggregator: ${aggregator}

data:
  batch_size: 74
  num_workers: 8

model:
  lr: 0

metadata:
  experiment_name: Prostate
  run_name: ???
  description: ???
  hyperparams:
    batch_size: ${data.batch_size}
    num_workers: ${data.num_workers}
    checkpoint: ${checkpoint}

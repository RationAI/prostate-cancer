# @package _global_

defaults:
  - /data/karolinska/full@data
  - _self_

combined_masks_dir: /mnt/data/Public/Public/prostate/PANDA/train_label_masks
untied_masks:
  tissue_mask: [1, 2]
  carcinoma_mask: [2]

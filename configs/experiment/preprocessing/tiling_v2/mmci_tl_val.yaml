# @package _global_

defaults:
  - /data/mmci_tl_val@data
  - /experiment/preprocessing/tiling_v2/base_512
  - _self_

overlappers:
  tissue_roi:
    _target_: preprocessing.tiling_v2.overlappers.TissueOverlapper
    roi_corners: ${roi.half}
    masks_uri: ${data.tissue_masks_uri}

# @package _global_

defaults:
  - /experiment/test/bag_of_embeddings/base
  - /model/pgp_hybird_mil_model@model
  - /model/checkpoints/mmci_tl/pgp_hybrid_mil@model_data
  - _self_

datamodule:
  test:
    uris:
      - ${test_data.tiles_filtered_w_pgp_uri_224}

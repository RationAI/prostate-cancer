# @package _global_

defaults:
  - /experiment/test/bag_of_embeddings/base
  - /model/virchow2_hybrid_mil_model@model
  - /model/checkpoints/mmci_tl/virchow2_hybrid_mil@model_data
  - _self_

datamodule:
  test:
    uris:
      - ${test_data.tiles_filtered_w_virchow2_uri_224}

# @package _global_

use_case: ???

model:
  lr: 0.0
  tl_threshold: ${tile_threshold}

tile_threshold: ${model_data.tile_threshold}
carcinoma_roi_t: ${test_data.thresholds.carcinoma_roi_t}
checkpoint: ${model_data.checkpoint}
model_name: ${model_data.model_name}
mean: ${model_data.stats.mean}
std: ${model_data.stats.std}
data_name: ${test_data.data_name}

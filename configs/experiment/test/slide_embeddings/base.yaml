# @package _global_

defaults:
  - /experiment/test/base
  - /datamodule/slide_embeddings_datamodule@datamodule
  - _self_

model:
  sl_threshold: ${slide_threshold}

datamodule:
  batch_size: 4
  num_workers: 8

  test:
    uris:
      - ${test_data.tiles_uri_224}

# @package _global_

defaults:
  - /experiment/test/slide_embeddings/base
  - /model/virchow2_mil_model@model
  - /model/checkpoints/mmci_tl/virchow2_mil@model_data
  - _self_

datamodule:
  test:
    embeddings_uri: ${test_data.virchow2_embeddings_uri}

# @package _global_

defaults:
  - /experiment/test/embeddings/base
  - /model/pgp_model@model
  - /model/checkpoints/pgp@model_data
  - _self_

datamodule:
  test:
    embeddings_uri: ${test_data.pgp_embeddings_uri}

# @package _global_

defaults:
  - /experiment/test/base
  - /datamodule/tile_embeddings_datamodule@datamodule
  - _self_

datamodule:
  batch_size: 1024
  num_workers: 8

  test:
    uris:
      - ${test_data.tiles_uri_224}

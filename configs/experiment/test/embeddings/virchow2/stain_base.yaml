# @package _global_

defaults:
  - /experiment/test/embeddings/base
  - /model/virchow2_model@model
  - /model/checkpoints/mmci_tl/virchow2_stain@model_data
  - _self_

datamodule:
  test:
    uris:
      - ${test_data.tiles_filtered_w_virchow2_uri_224}

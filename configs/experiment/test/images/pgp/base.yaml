# @package _global_

defaults:
  - /experiment/test/images/base
  - /model/pgp_full_model@model
  - /model/checkpoints/mmci_tl/pgp@model_data
  - _self_

datamodule:
  batch_size: 96
  num_workers: 8

  test:
    uris:
      - ${test_data.tiles_filtered_uri_224}

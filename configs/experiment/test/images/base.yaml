# @package _global_

defaults:
  - /experiment/test/base
  - /datamodule/images_datamodule@datamodule
  - _self_

datamodule:
  batch_size: 64
  num_workers: 8

  test:
    uris:
      - ${test_data.tiles_uri_512}
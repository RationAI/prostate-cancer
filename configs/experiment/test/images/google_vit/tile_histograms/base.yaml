# @package _global_

defaults:
  - /callbacks/tile_histograms@callbacks
  - /experiment/test/images/google_vit/base
  - _self_

datamodule:
  test:
    stratified_filter: true

use_case: "TL Histograms"

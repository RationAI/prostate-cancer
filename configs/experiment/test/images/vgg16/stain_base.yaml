# @package _global_

defaults:
  - /experiment/test/images/base
  - /model/vgg16_model@model
  - /model/checkpoints/mmci_tl/vgg16_stain@model_data
  - _self_

datamodule:
  test:
    uris:
      - ${test_data.tiles_filtered_uri_512}

# @package _global_

defaults:
  - /callbacks/tile_histograms@callbacks
  - /experiment/test/images/resnet50/stain_base
  - _self_

datamodule:
  test:
    stratified_filter: true

use_case: "TL Histograms"

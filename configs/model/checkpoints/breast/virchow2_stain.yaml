defaults:
  - base_foundation
  - _self_

tile_threshold: 0.5
slide_max_threshold: 0.5
slide_mpm_threshold: 0.5
slide_top_k_threshold: 0.5

checkpoint: "..."
public_checkpoint: "..."

model_name: "Virchow2 (Stain & Frozen)"

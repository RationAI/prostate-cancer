# @package _global_

# dataframe with column "path" containing selected slide paths and optionally "label" containing slide label
mpp: 0.46
tile_extent: ???
stride: ???
use_case: Tiling

metadata:
  hyperparams:
    tile_extent: ${tile_extent}
    stride: ${stride}
    mpp: ${mpp}

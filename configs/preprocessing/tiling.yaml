# @package _global_

# dataframe with column "path" containing selected slide paths and optionally "label" containing slide label
mpp: 0.46
tile_extent: ???
stride: ???
qc_masks_uri: ???
tissue_masks_uri: ???
annotation_masks_uri: ???


metadata:
  run_name: Tiling ${data.data_name}
  description: Tiling ${data.data_name}
  hyperparams:
    tile_extent: ${tile_extent}
    stride: ${stride}
    mpp: ${mpp}

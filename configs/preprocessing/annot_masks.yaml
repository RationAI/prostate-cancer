# @package _global_

defaults:
  - base
  - _self_

level: 5
max_concurrent: 100
output_path: ${prostate_cancer_artifacts}/annot_masks/${data_name}

metadata:
  run_name: Annotation Masks ${data_name}
  description: Annotation Masks ${data_name}

# @package _global_

defaults:
  - /default
  - _self_

thresholds: ${data.thresholds}

data: ???
use_case: ???

metadata:
  run_name: ${use_case} ${data.data_name}
  description: ${use_case} ${data.data_name}
